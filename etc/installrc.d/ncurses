
# There is no version number in the tarball name.
SOURCES_URI=http://invisible-island.net/datafiles/release/ncurses.tar.gz

function _build() {
	cd $CURRENT_VERSION_DIR

	# Try to use g++ first, otherwise build ncurses without it.
	./configure --prefix=$DOTSOFTWARE_ROOT_DIR && make install || \
	./configure --prefix=$DOTSOFTWARE_ROOT_DIR --without-cxx && make install
}
