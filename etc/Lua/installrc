
SOURCES_URI=http://www.lua.org/ftp/lua-${LUA_VERSION}.tar.gz

function _build() {
	cd $CURRENT_VERSION_DIR

	sed -i -e "s!INSTALL_TOP= /usr/local!INSTALL_TOP= $DOTSOFTWARE_ROOT_DIR!" Makefile

	make linux test && make install
}

