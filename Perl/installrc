SOURCES_URI=http://www.cpan.org/src/5.0/perl-5.18.2.tar.gz

function _build() {
	cd $CURRENT_VERSION_DIR
	./Configure -des \
		-Dprefix=$BASE_DIR/$CURRENT_VERSION_DIR \
		-Dscriptdir=$BASE_DIR/$CURRENT_VERSION_DIR/bin \
		&& make test \
		&& make install
}

