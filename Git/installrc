SOURCES_URI=http://git-core.googlecode.com/files/git-1.9.0.tar.gz

function _build() {
	cd $CURRENT_VERSION_DIR

	PERL=$(which perl)
	PYTHON=$(which python)

	./configure --prefix=$BASE_DIR/$CURRENT_VERSION_DIR --with-perl=$PERL --with-python=$PYTHON && make install
}

